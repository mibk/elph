-- class.php --
<?php

namespace Foo\Namespace;

$x = $this::class;
$y = self::foo()->class;

// Apparently "Enum" as a name is allowed.
class Enum
{
	const INTERFACE = 'Interface';
}

// Keyword is allowed as a named param.
register(class: 'Enum');
